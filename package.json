{
  "name": "shop-analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "etsy": "node src/cli.js",
    "test": "node --check src/server.js && node --check src/cli.js && node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
